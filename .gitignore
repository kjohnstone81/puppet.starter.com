# Dependencies
node_modules/
.pnp
.pnp.js

# Next.js
.next/
out/
build/
next-env.d.ts
*.tsbuildinfo

# Production
dist/

# Environment
.env
.env.local
.env*.local
!.env.example

# Terraform
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
tfplan
tfplan.*
# .terraform.lock.hcl is intentionally NOT ignored — commit it after the first
# `terraform init` so every run resolves the same provider versions.

# Credentials — never commit
*-sa-key.json
service-account*.json
application_default_credentials.json

# Logs / OS
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
*.pem

# Editor
.idea/
.vscode/
*.swp
